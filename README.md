# Student_Report
Created a Dataset Summary Report using Python and Pandas. Loaded and explored a CSV dataset by examining its first, last, and random rows, shape, columns, index, data types, and structure. Used `info()` and `describe()` for detailed analysis and identified missing values, duplicate records, and unique values.
